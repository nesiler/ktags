#!/usr/bin/env sh
# Reject AI attribution in commit messages. Activated by `make hooks` (core.hooksPath).
# CI re-runs the same check on every PR, so bypassing with --no-verify only delays the failure.
exec "$(git rev-parse --show-toplevel)/scripts/guard.sh" check-message "$1"
